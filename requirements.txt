# =============================================================================
# AI Supply Chain Optimizer — Python dependencies
# Python 3.11+
# Pin upper bounds on majors to keep builds reproducible.
# =============================================================================

# -----------------------------------------------------------------------------
# Core data stack
# -----------------------------------------------------------------------------
numpy>=1.26,<2.2
pandas>=2.2,<2.4
scipy>=1.12,<1.15
python-dateutil>=2.9
pytz>=2024.1

# -----------------------------------------------------------------------------
# Time-series forecasting
# -----------------------------------------------------------------------------
statsmodels>=0.14,<0.16        # SARIMA / SARIMAX
prophet>=1.1.5,<1.2            # Facebook Prophet (requires cmdstanpy)
cmdstanpy>=1.2,<1.3
holidays>=0.50                 # Holiday calendars used by Prophet/feature eng

# -----------------------------------------------------------------------------
# Machine learning
# -----------------------------------------------------------------------------
scikit-learn>=1.4,<1.6
xgboost>=2.0,<2.2
joblib>=1.3,<2.0

# -----------------------------------------------------------------------------
# Database / ORM
# -----------------------------------------------------------------------------
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9,<3.0
alembic>=1.13,<2.0             # Schema migrations

# -----------------------------------------------------------------------------
# API layer
# -----------------------------------------------------------------------------
fastapi>=0.110,<0.116
uvicorn[standard]>=0.27,<0.32
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
python-multipart>=0.0.9        # Form/file uploads if needed
orjson>=3.10                   # Faster JSON for FastAPI

# -----------------------------------------------------------------------------
# Dashboard
# -----------------------------------------------------------------------------
streamlit>=1.32,<1.40
plotly>=5.20,<6.0
altair>=5.2,<6.0               # Streamlit's default charting fallback

# -----------------------------------------------------------------------------
# External signals / HTTP
# -----------------------------------------------------------------------------
requests>=2.31,<3.0
httpx>=0.27,<0.29              # Async client + used by tests
tenacity>=8.2,<10.0            # Retries for flaky external APIs

# -----------------------------------------------------------------------------
# Cloud / deployment
# -----------------------------------------------------------------------------
boto3>=1.34,<2.0
botocore>=1.34,<2.0
mangum>=0.17,<0.18             # ASGI adapter for AWS Lambda

# -----------------------------------------------------------------------------
# Configuration / utilities
# -----------------------------------------------------------------------------
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0
typer>=0.12,<1.0               # CLI entry points for ETL / train
rich>=13.7,<14.0               # Pretty logs / tracebacks
loguru>=0.7,<1.0

# -----------------------------------------------------------------------------
# Testing
# -----------------------------------------------------------------------------
pytest>=8.1,<9.0
pytest-cov>=5.0,<6.0
pytest-asyncio>=0.23,<0.25
pytest-mock>=3.12,<4.0
freezegun>=1.4,<2.0            # Time mocking for forecast tests
faker>=24.0,<28.0              # Synthetic data fixtures

# -----------------------------------------------------------------------------
# Dev tooling (lint / format / type-check)
# -----------------------------------------------------------------------------
ruff>=0.4,<0.7
black>=24.3,<25.0
mypy>=1.9,<2.0
types-requests>=2.31
types-PyYAML>=6.0
pre-commit>=3.7,<4.0

# -----------------------------------------------------------------------------
# Notebooks (optional, for exploration in data/processed prototyping)
# -----------------------------------------------------------------------------
jupyterlab>=4.1,<5.0
ipykernel>=6.29,<7.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
